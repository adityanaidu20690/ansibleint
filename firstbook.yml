---
- name: first playbook
  hosts: localhost
  tasks:
  - name: ping the server
    ping:
  - name: print message
    debug: msg=addy
  - name: delete httpd
    yum:
     name: httpd
     state: present
  - name: service start
    service:
      name: httpd
      state: started  
