---
- name: first playbook
  hosts: localhost
  tasks:
  - name: ping the server
    ping:
  - name: stop
    yum:
     name: httpd
     state: stopped
  - name: delete httpd
    yum:
     name: httpd
     state: absent
